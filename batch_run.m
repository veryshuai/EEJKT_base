% This file simply allows me to call a function to run with batch pbs

% Load parameters
params

% Call calibration

% calibration_noprod(pop, 0);
% calibration(pop, 0);
% calibration_multistart(pop,0);
% calibration_multistart2(pop,0);
  calibration_mcmc
% mcmc_diffuse
